 Golf Cart Stock Management System ให้ดังนี้ครับ

1. ภาพรวมระบบ (System Overview)
เป็นเว็บแอปพลิเคชันสำหรับบริหารจัดการสต็อกอะไหล่รถกอล์ฟ รองรับการเบิกจ่าย (Withdraw), การรับเข้า (Receive), และการติดตามงานซ่อม (Maintenance Work Report - MWR) โดยมีระบบจัดการสิทธิ์ผู้ใช้งาน (Role-based Access Control) ที่ชัดเจน

เทคโนโลยีหลัก: Next.js (Fullstack), MariaDB (Database), Prisma (ORM), Tailwind CSS (UI)

2. กลุ่มผู้ใช้งาน (User Roles)
ระบบแบ่งผู้ใช้งานออกเป็น 4 ระดับ เพื่อควบคุม Workflow การทำงาน:

Staff (ช่าง/พนักงาน):
หน้าที่: เป็นผู้ปฏิบัติงานหน้างาน
สิทธิ์: เปิดใบแจ้งซ่อม (MWR), ขอเบิกอะไหล่, ดูประวัติการเบิกของตัวเอง
Stock Manager (ผู้จัดการสต็อก):
หน้าที่: ดูแลคลังสินค้าโดยเฉพาะ
สิทธิ์: รับอะไหล่เข้าคลัง (Receive), ปรับยอดสต็อก (Adjust), จัดการข้อมูลอะไหล่ (เพิ่ม/ลบ/แก้ไข), ดูแลรายการเบิกจ่ายวัสดุ
Manager (ผู้จัดการ):
หน้าที่: ผู้อนุมัติและดูภาพรวม
สิทธิ์: อนุมัติใบแจ้งซ่อม (Approve MWR), ดูรายงานและสถิติ (Reports), ตรวจสอบภาพรวมการทำงาน (จะไม่เข้าไปยุ่งกับยอดสต็อกโดยตรง)
Admin (ผู้ดูแลระบบ):
หน้าที่: ดูแลระบบทั้งหมด
สิทธิ์: จัดการผู้ใช้งาน (User Management), แก้ไขการตั้งค่าระบบ, และเข้าถึงได้ทุกฟีเจอร์
3. เวิร์คโฟลว์การทำงานหลัก (Core Workflows)
🔄 A. Flow การเบิกจ่ายอะไหล่และงานซ่อม (MWR Workflow)
เป็นหัวใจหลักของระบบ เริ่มต้นจากช่างต้องการอะไหล่ไปซ่อมรถ

Request (สร้างคำขอ):
Staff สร้างใบแจ้งซ่อม (MWR) ระบุรายละเอียดรถกอล์ฟ (Serial No.) และ อะไหล่ที่ต้องการเบิก
สถานะเริ่มต้น: PENDING
Approval (การอนุมัติ):
Manager ได้รับการแจ้งเตือน เข้ามาตรวจสอบรายละเอียด
หากอนุมัติ -> สถานะเปลี่ยนเป็น APPROVED (หรือ IN_PROGRESS)
หากไม่อนุมัติ -> สถานะเปลี่ยนเป็น REJECTED พร้อมเหตุผล
Disbursement (การจ่ายของ):
เมื่ออนุมัติแล้ว ระบบจะตัดสต็อกอะไหล่ (Transaction Type: WITHDRAW)
Stock Manager หรือระบบตัดสต็อกตามรายการที่เบิก
Completion (ปิดงาน):
เมื่อซ่อมเสร็จ Staff หรือ Manager อัปเดตสถานะเป็น COMPLETED
ระบบบันทึกประวัติลงใน SerialLog และ Transaction History
📦 B. Flow การจัดการสต็อก (Stock Management Workflow)
Receiving (รับของเข้า):
Stock Manager บันทึกการรับอะไหล่ใหม่เข้าเติมในคลัง
ระบุจำนวนและราคาต้นทุน (ถ้ามี)
ระบบบันทึก Transaction Type: RECEIVE และเพิ่มยอด Quantity ในตาราง Part
Adjustment (ปรับยอด):
กรณีของหายหรือชำรุด Stock Manager ทำการปรับยอด (Transaction Type: ADJUST)
Low Stock Alert (แจ้งเตือนของใกล้หมด):
เมื่อยอดคงเหลือต่ำกว่า minStockLevel ระบบจะแจ้งเตือนให้สั่งซื้อเพิ่ม
👥 C. Flow การจัดการผู้ใช้ (User Management)
Admin สร้างบัญชีผู้ใช้ใหม่ หรือ แก้ไขสิทธิ์ (Role)
สามารถกำหนด Permission ย่อยได้ (เช่น View หรือ Action ในแต่ละหน้า) ตามตาราง UserPermission และ RolePermission
4. โครงสร้างข้อมูลที่สำคัญ (Data Model)
User: เก็บข้อมูลพนักงานและ Role
Part: เก็บรายละเอียดอะไหล่ ยอดคงเหลือ และจุดจัดวาง (Location)
